#include<stdio.h>
int x,y;
int map[][10] = {0};

int main(){
	
	return 0;
} 

void init(){
	x=50;
	y=25;
} 

void map_print(){
	for (x=0,x<50,x++){
		for(y=0,y<25,y++)
			if(map==0)
			if(map==1)
			if(map==2)
			if(map==3)
	}
	
}
